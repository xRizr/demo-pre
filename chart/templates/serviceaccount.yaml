apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
  annotations:
    # The webhook (workload-identity-enabled on cluster) reads this and projects
    # a JWT into the pod that maps to the Azure UAMI with this client_id.
    azure.workload.identity/client-id: {{ .Values.workloadIdentityClientId | quote }}
